# 更新日志

本文档记录 Teacher AI 的主要变更。项目仍在按实际教学使用场景继续调整，更新内容以功能、部署和稳定性为主。

---

## [2026-06-25] 公开仓库整理与本地运行兼容

### 发布
- 整理 GitHub 仓库首页文档，补充功能说明、部署步骤、数据安全说明和常见问题。
- 保留 Apache License 2.0，明确项目可以在许可证范围内使用和二次开发。
- 将运行时数据目录 `data/` 加入 `.gitignore`，避免误提交学生数据、日志和本地 SQLite 数据库。

### 调整
- 移除前端访问验证码遮罩。公开仓库不再保留本地手机号式验证码，避免误导为正式登录鉴权。
- README 中补充 `data/` 自动创建说明：首次访问时会创建 `feedback.db`，前提是 PHP 对项目目录有写权限。
- 本地开发环境切换到 PHP 8.2，作为推荐测试版本。

### 修复
- 兼容 PHP 8.5 的弃用提醒处理，避免 `E_DEPRECATED` 被全局错误处理器转成 API 500。
- 本地诊断接口验证通过：页面、SQLite 初始化和基础 API 均可正常访问。

---

## [2026-06-09] 模型配置与诊断能力增强

### 新增
- 增加模型可用性检测，支持验证当前 API Key 是否能访问所选模型。
- 增加 Token 统计入口，方便查看历史生成次数和累计消耗。
- 增加配额信息入口，用于辅助判断生成失败是否和账号用量有关。

### 调整
- 模型列表更新为通义千问常用模型与 latest 系列：
  - `qwen-turbo`
  - `qwen-plus`
  - `qwen-max`
  - `qwen-long`
  - `qwen-plus-latest`
  - `qwen-max-latest`
  - `qwen3.6-flash`
  - `qwen3.7-plus`
- API Key 保存后只展示掩码，不在前端返回完整密钥。
- 生成时根据目标字数动态调整 `max_tokens`，减少长反馈被截断的情况。

### 修复
- 修复旧模型名导致的 404 问题，改用当前配置中的有效模型名称。
- 优化错误兜底，后端异常尽量返回 JSON，减少前端 `resp.json()` 解析失败。

---

## [2026-06-08] 学生档案、历史记录与反馈生成优化

### 新增
- 增加学生档案视图，按学生聚合反馈次数、最近反馈时间、历史内容和 Token 消耗。
- 增加历史记录详情查看、搜索、单条删除和批量删除。
- 增加 CSV 导出，便于备份或做后续统计。
- 增加一对一、小班课、大班课、线上课、考前复习等教学场景选项。

### 调整
- 小班课/大班课支持多学生批量生成，每位学生可单独填写水平等级和课堂维度。
- 反馈 Prompt 增加年级知识范围约束，减少生成建议时推荐超纲方法。
- 输出格式统一为“授课内容 / 课堂表现 / 作业”，更适合复制给家长。
- 调整反馈语气：避免夸张、卖萌、空泛鼓励和未提供细节的编造。

### 修复
- 修复历史记录加载失败的问题。
- 修复学生档案弹窗和历史弹窗之间切换闪动的问题。
- 修复未选择学生水平等级时仍可生成的问题。
- 修复字数范围偶尔被忽略的问题。

---

## [2026-06-08] 前端文件拆分与基础结构整理

### 调整
- 将页面样式拆分到 `css/style.css`。
- 将前端脚本拆分为：
  - `js/dimensions.js`：课堂评价维度定义。
  - `js/utils.js`：公共工具函数。
  - `js/main.js`：页面交互和业务逻辑。
- 后端保留单入口 `api.php`，通过 `action` 参数区分配置、生成、历史、诊断和导出等操作。
- SQLite 初始化逻辑集中在 `database.php`，首次运行时自动创建表结构。

### 新增
- 内置课堂表现维度，包括作业完成、听课状态、课堂互动、犯困/走神、随堂练习、基础知识、公式掌握、做题速度、解题思路、计算能力、学习习惯、进步趋势和数学思维等。
- 增加快捷评价模板，便于快速套用常见课堂表现组合。

---

## [早期版本] 基础功能

### 新增
- 基于通义千问 API 生成数学课后反馈。
- 支持保存 API Key、模型、Temperature 和最大输出 Token。
- 使用 SQLite 保存反馈历史。
- 支持复制反馈内容，并提供微信、企业微信、钉钉和系统分享入口。
